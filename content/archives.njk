---
layout: layouts/base.njk
title: Archives
permalink: /archives/
---

<div class="wrapper flow">
  <h1>Archives</h1>
  
  <p>Tous les articles du blog, organisés par année.</p>
  
  {% set currentYear = "" %}
  {% for post in collections.posts | reverse %}
    {% set postYear = post.date | readableDate("yyyy") %}
    {% if postYear != currentYear %}
      {% if currentYear != "" %}
        </ul>
      </section>
      {% endif %}
      <section class="year-section">
        <h2>{{ postYear }}</h2>
        <ul class="posts-list">
      {% set currentYear = postYear %}
    {% endif %}
    <li>
      <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate("dd MMM") }}</time>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </li>
  {% endfor %}
  
  {% if currentYear != "" %}
    </ul>
  </section>
  {% endif %}
</div>
